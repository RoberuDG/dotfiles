#!/bin/bash
# Simple weather indicator for polybar
# Shows weather icon and temperature

CITY="Buenos Aires"
WEATHER=$(curl -s "wttr.in/${CITY// /%20}?format=%c+%t" 2>/dev/null)

if [[ -z "$WEATHER" || "$WEATHER" == " " ]]; then
    echo " --"
else
    echo "$WEATHER"
fi
